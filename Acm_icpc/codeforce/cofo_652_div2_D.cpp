#include <iostream>

#define DIV 1000000007

using namespace std;

int main(void) {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
	
	int t_case; cin >> t_case;
	for (int i = 0; i < t_case; i++) {
		int level_of_rdb; cin >> level_of_rdb;
		

	}

	return 0;
}