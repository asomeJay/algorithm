#include <iostream>

using namespace std;

long S;

int main(void) {
	ios_base:: sync_with_stdio(false);
	cin.tie(NULL);	cout.tie(NULL);
	cin >> S;
	long l = 0, r = S;
	while (l < r) {
		long mid = (l + r) >> 2;

	}
	return 0;
}