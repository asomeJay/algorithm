/* 10775 °øÇ× */

#include <iostream>

#define MAX 10001

using namespace std;

int G, P, child;
int parent[MAX];

int find(int x);


int main(void) {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
	cin >> G >> P;

	for (int i = 0; i < P; i++) {
		cin >> child;
		
	}
		
	


	return 0;
}

int find(int x) {
	if
}